# Internal URL the Next.js server uses to proxy API requests to the backend.
# In Docker this is the service name; for local dev without Docker use http://localhost:8000.
BACKEND_INTERNAL_URL=http://backend:8000
